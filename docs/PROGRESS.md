# Progress

- Current issue: #36 is the sole hardening gate after #34/#35 closed at pushed evidence commit `b47b5f4`; #37 remains blocked.
- Hypothesis: the integrated production scene preserves selection, semantic/event/manifestation hashes, represented totals, and interaction targets across fallback/baseline/showcase while meeting the frozen #32 Canvas budgets. Any over-budget production-scene metric, observer/quality drift, real-soak frame or retained-heap regression, request/console error, or browser/accessibility/fallback failure falsifies it.
- Last green commit: `b47b5f4` (issues #34/#35 evidence, pushed to `origin/main`; root `pnpm check` passed 20 files / 109 tests and the full browser matrix passed 30 applicable cases with 13 declared project skips).
- Evidence produced: #34 fixed-time commute, meeting, festival, baseline, and closure captures now use the viewport shell; maximum retained renderer frame is 1.11 ms and retained heap is 26.32 MiB. #35 production captures prove a 1,418×776 desktop renderer in a 1,440×900 viewport, no document scroll, a 725 px-tall Pixel 7 renderer, 44 px minimum visible buttons, keyboard preview without selection/hash drift, follow/scrub/exit, touch picking, a contained evidence drawer, forced colors at 200% text, no external requests, and no serious/critical axe findings.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: none. Root `main` is the sole #36 integration authority.
- Next action: add the cheapest production-scene quality benchmark assertion, capture same-profile before measurements, then implement only measured hardening and run the fresh-clone outer loop including the real 30-minute soak.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. The #30/#31 structural seam uses `position`, ordered edge `path`, `adjacentEdgeIds`, `entranceNodeId`, and `closedInBranch`; no shared contract edit was required. Issue #28 remains deferred and untouched.
- Blockers: none. Live milestone audit found #30–#32 closed and no separate unresolved #30–#33 P0 defect. A usable WebGPU adapter remains unavailable on the committed headless profile, so Canvas is authoritative and no WebGPU visual/performance claim is made. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
