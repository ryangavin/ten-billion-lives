# Progress

- Current issue: #37 final M4 release gate is green; its evidence and documentation are ready for the closing commit and issue comment. Issues #29–#36 are closed, and #37 is the only open P0 visualization issue.
- Hypothesis: the complete local living-city experience fits the viewport, preserves exact authority and observer semantics, remains coherent through playback/branches/replay/fallback, and meets its accessibility, compatibility, and performance budgets. The final clean-checkout evidence supports this hypothesis.
- Last green implementation commit: `a742dd5`. A new remote clone passed the exact bootstrap, `pnpm check` with 20 files / 110 tests, `pnpm start`, seven-vector replay, the serialized 45-case browser matrix, the full handoff capture, and affected production benchmarks.
- Evidence produced: #37 one-take journey video and transcript, 13 desktop/narrow/state captures, exact replay, 45-case Chromium/WebKit/mobile summary, current tier/experience measurements, live issue/scope audit, and links to the applicable real 1,800,025 ms soak. Desktop and narrow documents equal their viewports with 1418×776 and 398×725 renderers respectively.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: none. The fresh remote clone is the sole #37 evidence authority.
- Next action: commit the #37 evidence/docs, close #37 with exact commands and artifact links, and verify that no P0 visualization issue remains open.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. The #30/#31 structural seam uses `position`, ordered edge `path`, `adjacentEdgeIds`, `entranceNodeId`, and `closedInBranch`; no shared contract edit was required. Issue #28 remains deferred and untouched.
- Blockers: none for #37. A usable WebGPU adapter remains unavailable on the committed headless profile, so Canvas is authoritative and no WebGPU visual/performance claim is made. Firefox remains unavailable and explicitly unvalidated. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
