# Progress

- Current issue: #33 is the sole integration issue and is at closing evidence review; #29–#32 are closed and pushed, while #34/#35 remain queued until #33 closes.
- Hypothesis: the integrated #30 city, #31 explicit-time trajectories, and #32 literal-person renderer can replace hash-only production street placement through the readonly #29 seams without changing simulation authority, exact population conservation, replay hashes, deep links, branch behavior, or independent-observer semantics. Any semantic/hash drift, invalid route, discontinuous playback, unstable pick, missing fallback, over-budget result, or visible P0 defect falsifies it.
- Last green commit: `72670b7` (issue #33 acceptance evidence map, pushed to `origin/main`; root `pnpm check` passed 20 files / 109 tests and the full browser matrix passed 28 applicable cases with 12 declared project skips).
- Evidence produced: #33 integrates city geometry, explicit trajectory poses, weighted manifestations, semantic zoom, exact Canvas-coordinate picking, independent observer comparison, continuous playback, direct seek/replay, and renderer recovery. The mandatory fresh-clone gate at `72670b7` passed pinned frozen install, root checks, seven byte-identical duplicate vectors, the complete browser matrix without retries, and the committed production/fallback capture. Fresh-clone measurements were 2,727.42 ms cold city, 1,842.79 ms zoom p95, 1,293.27 ms pick, 2,626.68 ms observer B, 0.96 ms worst retained frame, and 64.85 MiB retained heap; all provisional budgets passed. The final full-duration review then caught one evidence-only gray compositor frame, invalidating that gate; the harness now records video and captures stills in fully independent contexts, and the replacement video passed both 1 fps full-duration and 4 fps tail inspection.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: none. Root `main` remains the sole #33 integration authority.
- Next action: isolate every still capture from the video-recording contexts after the final review exposed one remaining gray compositor frame, push the evidence-only correction, restart the entire #33 outer loop from a new remote clone, close #33 only if it remains green, then begin the explicitly independent #34/#35 wave.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. The #30/#31 structural seam uses `position`, ordered edge `path`, `adjacentEdgeIds`, `entranceNodeId`, and `closedInBranch`; no shared contract edit was required. Issue #28 remains deferred and untouched.
- Blockers: none. Live milestone audit found #30–#32 closed and no separate unresolved #30–#33 P0 defect. A usable WebGPU adapter remains unavailable on the committed headless profile, so Canvas is authoritative and no WebGPU visual/performance claim is made. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
