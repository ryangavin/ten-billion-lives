# Progress

- Current issue: #36 hardening evidence is green at release candidate `743ec52`; documentation/evidence closure is in progress. #37 is next and remains blocked until #36 closes.
- Hypothesis: the integrated production scene preserves selection, semantic/event/manifestation hashes, represented totals, and interaction targets across fallback/baseline/showcase while meeting the frozen Canvas and long-session budgets. The clean-checkout evidence now supports this hypothesis.
- Last green commit: `743ec52` (viewport cache retention fix, pushed to `origin/main`). A new remote clone passed `pnpm check` with 20 files / 110 tests, seven-vector replay, all 12 benchmark commands, and the serialized 45-case browser matrix.
- Evidence produced: #36 production fallback/baseline/showcase captures, exact seven-vector replay, 45-case Chromium/WebKit/mobile summary, and a real 1,800,025 ms soak. The soak recorded 1.32 ms maximum frame p95, 35.91 MiB maximum retained heap, 13.88 MiB growth, 0.89 final/initial frame ratio, 35 interactions, three context recoveries, two background resumes, exact two-observer/population checks every minute, and zero console/page/network errors.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: none. Root `main` is the sole #36 integration authority.
- Next action: commit the #36 evidence/docs, close #36 with exact commands and artifact links, then begin the independent #37 release audit.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. The #30/#31 structural seam uses `position`, ordered edge `path`, `adjacentEdgeIds`, `entranceNodeId`, and `closedInBranch`; no shared contract edit was required. Issue #28 remains deferred and untouched.
- Blockers: none for #36. A usable WebGPU adapter remains unavailable on the committed headless profile, so Canvas is authoritative and no WebGPU visual/performance claim is made. Firefox remains unavailable and explicitly unvalidated. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
