# Progress

- Current issue: parallel wave #30/#31/#32 is active; root `main` at `/Users/ryan/The Source/sim` is the integration authority and will integrate #30 first.
- Hypothesis: the frozen #29 seams and disjoint path ownership permit three independently green commits that root can integrate in #30 → #31 → #32 order without contract ambiguity or semantic regression. An overlapping implementation edit, incompatible interface assumption, authority leak, unmeasured visual budget, or affected-check failure falsifies it and serializes the conflicting portion.
- Last green commit: `aec79e0` (issue #29 contract checkpoint, pushed to `origin/main`, `pnpm check` green, issue closed).
- Evidence produced: `docs/LIVING_CITY.md` freezes the product/storyboard, 2.5D rationale, readonly interfaces, time/range behavior, playback and reduced-motion rules, crowd honesty, evidence schema, measurement method, ownership, and fallbacks. `docs/evidence/issue-29/living-city-wireframe.svg` was rendered in Chromium and its 1600 × 900 PNG was inspected with no P0 directional defect; the narrow callout risk is owned by #35. Issue #29 is closed with commit, command, test, hash, and artifact evidence.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: #30 owner `/root/issue_30_city`, branch `issue-30-city`, worktree `/private/tmp/ten-billion-lives-m4-30`; #31 owner `/root/issue_31_trajectories`, branch `issue-31-trajectories`, worktree `/private/tmp/ten-billion-lives-m4-31`; #32 owner `/root/issue_32_renderer`, branch `issue-32-renderer-spike`, worktree `/private/tmp/ten-billion-lives-m4-32`. All started from `aec79e0`; their sole paths and the two root-owned `packages/manifest/src/index.ts` export merges are recorded in the contract and live issue start comments.
- Next action: accept only committed green lane handoffs; inspect each diff and evidence locally; integrate #30, run city-focused plus root/visual checks, push and close #30; repeat for #31 then #32. Revalidate all three together before starting #33.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. Issue #28 remains deferred and untouched.
- Blockers: none. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
