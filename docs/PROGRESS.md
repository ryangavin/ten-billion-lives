# Progress

- Current issue: #29 — root `main` at `/Users/ryan/The Source/sim`; no parallel lane is active until the contract is closed and pushed.
- Hypothesis: one versioned living-city contract plus an inspected 2.5D wireframe can freeze city, visual-time, trajectory, renderer, picking, accessibility, evidence, benchmark, and package seams so #30/#31/#32 can work without independently changing shared contracts. Ambiguous ownership, authority leakage, conflicting touched paths, an untestable injected-clock case, or an unowned risk falsifies it.
- Last green commit: `e57e0fc` (local and remote main matched at #29 start). Pre-change `pnpm check` passed formatting, docs, lint, types, 15 files / 76 tests, and build; #27 is closed with `docs/evidence/issue-27/INDEX.md`.
- Evidence produced: `docs/LIVING_CITY.md` freezes the product/storyboard, 2.5D rationale, readonly interfaces, time/range behavior, playback and reduced-motion rules, crowd honesty, evidence schema, measurement method, ownership, and fallbacks. `docs/evidence/issue-29/living-city-wireframe.svg` was rendered in Chromium and its 1600 × 900 PNG was inspected with no P0 directional defect; the narrow callout risk is owned by #35.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Planned isolated lanes: #30 `issue-30-city` at `/private/tmp/ten-billion-lives-m4-30`; #31 `issue-31-trajectories` at `/private/tmp/ten-billion-lives-m4-31`; #32 `issue-32-renderer-spike` at `/private/tmp/ten-billion-lives-m4-32`. Their sole paths and the two root-owned `packages/manifest/src/index.ts` export merges are recorded in the contract. Fall back to serial execution if isolation is unavailable.
- Next action: run the focused contract/docs checks, validate artifact hashes and scope, run `pnpm check`, commit/push the green #29 increment, close #29 with evidence, then create the isolated wave worktrees from that pushed commit.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. Issue #28 remains deferred and untouched.
- Blockers: none. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
