# Progress

- Current issue: parallel wave #31/#32 remains active; #30 is closed on integrated root `main`, and #31 is the next integration issue.
- Hypothesis: the frozen #29 seams and disjoint path ownership permit three independently green commits that root can integrate in #30 → #31 → #32 order without contract ambiguity or semantic regression. An overlapping implementation edit, incompatible interface assumption, authority leak, unmeasured visual budget, or affected-check failure falsifies it and serializes the conflicting portion.
- Last green commit: `e6a4dc7` (issue #30 integrated benchmark recapture, pushed to `origin/main`; root `pnpm check` passed 16 files / 80 tests; issue closed).
- Evidence produced: #29 froze the shared contract in `docs/LIVING_CITY.md`. #30 now provides canonical `CityProjection` hash `bc0c9ac7fa5ed058`, golden/property/connectivity proof, 36 nodes/60 edges and seven semantic anchors, an inspected 1200 × 1000 debug map, and a passing root same-profile benchmark in `docs/evidence/issue-30/README.md`.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: #31 owner `/root/issue_31_trajectories`, branch `issue-31-trajectories`, worktree `/private/tmp/ten-billion-lives-m4-31`; #32 owner `/root/issue_32_renderer`, branch `issue-32-renderer-spike`, worktree `/private/tmp/ten-billion-lives-m4-32`. Both started from `aec79e0`. #30 owner `/root/issue_30_city` handed off clean commits and is complete; its worktree is retained until the wave integrates.
- Next action: require the missing #31 real-Chromium playback/seek artifact, then inspect and integrate its committed handoff while reconciling only the intended manifest export overlap. Run trajectory/playback/vector/benchmark/browser and root checks, push and close #31; repeat for #32, then revalidate all three together before #33.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. The #30/#31 structural seam uses `position`, ordered edge `path`, `adjacentEdgeIds`, `entranceNodeId`, and `closedInBranch`; no shared contract edit was required. Issue #28 remains deferred and untouched.
- Blockers: none. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
