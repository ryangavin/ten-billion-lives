# Progress

- Current issue: #33 serial production integration is unblocked; the #29–#32 contract/city/trajectory/renderer wave is closed on pushed root `main`.
- Hypothesis: the integrated #30 city, #31 explicit-time trajectories, and #32 literal-person renderer can replace hash-only production street placement through the readonly #29 seams without changing simulation authority, exact population conservation, replay hashes, deep links, branch behavior, or independent-observer semantics. Any semantic/hash drift, invalid route, discontinuous playback, unstable pick, missing fallback, over-budget result, or visible P0 defect falsifies it.
- Last green commit: `1710964` (issue #32 integrated recapture, pushed to `origin/main`; root `pnpm check` passed 19 files / 105 tests; issue closed).
- Evidence produced: #30 provides canonical `CityProjection` hash `bc0c9ac7fa5ed058`, topology/property proof, inspected debug map, and passing generation/heap budgets. #31 provides pure trajectory/vector and playback reducers, ≤1 cm boundary continuity, identical replay/browser runs, and 15,077 q/s p50 with zero retained rows. #32 provides inspected literal-person day/evening/selection/context-loss frames and a 5.04-second loop; its frozen Canvas tiers are 128/256/512 with frame p95 0.70/1.00/2.20 ms, zero external requests/errors, and an explicit no-usable-WebGPU-adapter limitation.
- Frozen interfaces: schema-1 `CityProjection`/`cityHash`; `(tick, phasePermillion)` `VisualTime`; pure `PedestrianTrajectoryQuery`/`PedestrianPose`/`trajectoryHash`; immutable `LivingCityScene`; app-only camera/playback; stable-key `PickResult`; scene-derived `LivingCitySummary`. Presentation phase, clocks, camera, renderer, and pixels remain outside state/event/manifestation authority.
- Active isolated lanes: none. Completed #30/#31/#32 worktrees remain retained as read-only handoff history; root `main` is the sole #33 integration authority.
- Next action: post the #33 start hypothesis, create a fresh clone from `origin/main`, run the mandatory clean-install/root and duplicate-vector preflight, then implement the smallest production city/trajectory/renderer seam with focused tests before exercising the uninterrupted browser journey and integrated benchmark gate.
- Decisions: keep 24 authoritative hourly ticks; use integer parts-per-million presentation phase and injected monotonic clock anchors; hidden time never catches up; use a dominant orthographic 2.5D fictional city; keep Canvas journey-complete; let #32 measure before freezing literal-figure density budgets. The #30/#31 structural seam uses `position`, ordered edge `path`, `adjacentEdgeIds`, `entranceNodeId`, and `closedInBranch`; no shared contract edit was required. Issue #28 remains deferred and untouched.
- Blockers: none. A usable WebGPU adapter remains unavailable on the committed headless profile, so Canvas is authoritative and no WebGPU visual/performance claim is made. Server, networking, remote maps/data, deployment, and other prohibited scope remain excluded.
